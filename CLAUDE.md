# CLAUDE.md

This project follows the guidance in [`AGENTS.md`](AGENTS.md). Read it first — it is the single
source of truth for constraints, layout, build/verify steps and conventions. Do not duplicate that
content here, so the two never diverge.
